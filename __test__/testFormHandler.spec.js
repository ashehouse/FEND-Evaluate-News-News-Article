  
import { handleSubmit } from '../src/client/js/formHandler';
import { mockApi } from '../src/__mocks__/mockAPI';



describe('Test that handleSubmit() funcion works', () => {
    test('it should check the url and post to the server', async () => {
        const input = formHandler(../);
        console.log(input);
    });
    //test function
});